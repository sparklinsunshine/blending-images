# blending-images

A small collection of image blending experiments.

This workspace contains scripts that:

- load a source image with alpha transparency
- warp it onto a destination photo with a perspective transform
- match sharpness, brightness, and contrast
- blend the result using soft alpha compositing plus texture modulation

Files in this folder are for experimenting with different blending approaches.

